var program = function(input) {

	var xor = function(a,b) {
		return (a || b) && !(a && b);
	};

	var A = input.A;
	var B = input.B;
	var C = xor(A, B);

	return {
		A: A,
		B: B,
		C: C
	}
};

var input = {
	A: true,
	B: true
};

program(input);
