var program = function(input) {

	var A = input.A;
	var Mab = input.Mab;
	var Mbc = input.Mbc;
	
	var B = Mab ? A : !A;
	var C = Mbc ? B : !B;

	return {
		A: A,
		B: B,
		C: C
	};
};

var input = {
	A: false,
	Mab: true,
	Mbc: false
};

program(input);
